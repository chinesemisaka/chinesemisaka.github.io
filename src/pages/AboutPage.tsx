export function AboutPage() {
  return (
    <div className="mx-auto flex min-h-screen w-full max-w-3xl flex-col gap-4 p-6">
      <h1 className="text-2xl font-semibold">About This Template</h1>
      <p className="text-muted-foreground">
        Copy this folder for every new project to skip repeated environment setup.
      </p>
    </div>
  )
}
